树节点的存储

■指针存树，一般都是先把输入的数据存在数组，再把数组内的数取出来，生成一棵树。没有树节点编号的概念。
例如:一般二叉树(主要是二叉树的重建)，bst 。

■数组存树，直接将输入的数据存入数组树，有下标的概念: 用结构体内的int v;(或 int data;)表示树节点的编号 ; 用数组的下标表示树节点的编号,前提是树节点的编号是连续的。
例如:树的存储。
此时，就有点类似于图的存储，注意图的边有可以无向的，而树的边肯定有向，(解决途径为:vis[ ],防止回头)。